{
  "name": "@tailflow/dotenv",
  "description": "A simple package for managing .env file entries",
  "version": "1.2.1",
  "files": ["*"],
  "author": "Aleksei Zarubin alex@zarubin.co",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/tailflow/dotenv.git"
  },
  "license": "MIT",
  "scripts": {
    "build": "tsc -b tsconfig.json",
    "watch": "tsc -b --watch tsconfig.json",
    "test": "jest",
    "publish": "npm run build && cp package.json ./build/package.json && cd build && npm publish"
  },
  "devDependencies": {
    "@types/node": "^16.0.0",
    "@types/jest": "^26.0.20",
    "typescript": "4.4.4",
    "jest": "^26.6.3",
    "ts-jest": "^26.5"
  }
}
